# OurNest – Apartment Management App

Monorepo containing:

- `backend/` – FastAPI service (OTP authentication)
- `frontend/` – React (Vite) web app

---

## 1. Backend (FastAPI)

### Setup

```bash
cd backend
python -m venv venv
# Windows:
# venv\Scripts\activate
# macOS/Linux:
# source venv/bin/activate

pip install -r requirements.txt
