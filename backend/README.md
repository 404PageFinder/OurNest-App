# OurNest Backend (FastAPI)

## Setup
python -m venv venv
source venv/bin/activate  (Mac/Linux)
venv\Scripts\activate     (Windows)

pip install -r requirements.txt

## Run server
uvicorn main:app --reload
